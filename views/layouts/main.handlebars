<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gourmet</title>
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma-rtl.min.css">
</head>

<body>
  <div>
<header>
  <nav class="navbar has-background-light level" role="navigation" aria-label="main navigation">
    <div class="navbar-start">
      <div class="navbar-item">
        <a href="/" class="navbar-item level-item">Home</a>
        {{#if loggedIn}}
        <a href="/myRecipes" class="navbar-item">My Recipes</a>
        {{/if}}
      </div>
    </div>
    <div class="navbar-item">
      <h2 class="title">Welcome to Gourmet!</h2>
    </div>
    <div class="navbar-end">
      <div class="navbar-item">
        {{#if loggedIn}}
      <a href="/" id="logout" class="button is-link">Logout</a>
      {{else}}
        <div class="buttons">
          <a class="button is-link" href="/signUp">
            <strong>Sign up</strong>
          </a>
          <a class="button is-light" href="/login">
            <strong>Log in</strong>
          </a>
        </div>
        {{/if}}
      </div>
    </div>
  </nav>
</header>
    <main>
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>


  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
