<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gourmet</title>
</head>

<body>
  <div>
    <header>
      <h1>
      Welcome to Gourmet!
      </h1>
      <p><a href="/">Home</a></p>
      <p><a href='/'>My Recipes</a></p>
      {{#if loggedIn}}
      <a href="/" id="logout">Logout</a>
      {{else}}
      <p><a href="/login">User Login</a><p>
      {{/if}}
    </header>
    <main>
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>


  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
